# CLAUDE.md — 项目接续与工作规约

> 本文件是**新会话的启动锚点**。Claude 每次进入本项目自动读取它。
> 目标:任何时刻停下来,新会话都能凭本文件 + `docs/ROADMAP.md` 无缝接上。

## 项目是什么

海外工商业储能(C&I ESS)出口企业的 **AI 营销/销售数字化系统**。原始需求 = `需求文档.md`(公司需求基线,事实源)。以"需求驱动产品开发 + superpowers 工作流"方式推进,每个闭环/MVP 独立开发。

## 文件地图(权威顺序,冲突时靠前覆盖靠后)

| 文件 | 作用 | 冲突时 |
|---|---|---|
| `需求文档.md` | 原始需求基线(事实源) | — |
| `系统总纲.md` | 总纲:业务边界、飞轮、闭环 L1~L5、路线图 P0~P3 | **最高**,spec 冲突先回总纲对齐 |
| `docs/ROADMAP.md` | 主执行图:全系统模块/步骤/MVP 节点 + 实时状态 | 总纲之下;记录"当前节点" |
| `docs/superpowers/specs/` | 各节点 spec(一个闭环/MVP 一个) | 不得与总纲冲突 |
| `docs/superpowers/plans/` | 各节点实现计划 | 服从其 spec |
| `docs/decisions/` | 决策记录(默认值/选型/结论) | 记录"为什么",可被新决策推翻 |
| `README.md` | 入口索引(本文件的外层入口) | — |

## 接续协议(停止 → 接上)

1. 新会话:读完本文件 → 读 `docs/ROADMAP.md` 顶部「当前指针」
2. 找到状态为 `▶ in_progress`(或最近完成的 `✔`)的节点 → 读它对应的 spec/plan
3. 若一切已提交、无 in_progress 节点 → 从 ROADMAP 里下一个 `⬜` 节点开始(先跑该节点流程)
4. 每完成一个节点/阶段:更新 ROADMAP 状态、提交、**更新本文件「当前指针」**
5. 变更走 git;大改流程先讨论,不直接推翻总纲

## 开发纪律(每个节点必守)

- **入口流程**:新功能/节点一律先走 superpowers 的 brainstorm(含 HARD-GATE:未批不得动手)→ 大节点写 spec → writing-plans → 实现。禁止跳过审批直接写代码
- **Git/分支**:每个 MVP/节点独立分支或 worktree;遵守 `using-git-worktrees` / `finishing-a-development-branch`;`spec/plan/决策` 都要 commit
- **红线**(详见总纲 §8,所有节点生效):
  1. 储能技术/合规内容**只能引用事实卡片(Facts),禁止 AI 自创技术表述**
  2. 复杂售前默认**人机协同**,报价/承诺/技术判定由人确认
  3. 对外内容/多语言**必须人审后发布**
  4. 数据合规、海外隐私、账号权限、审计
  5. 上线节点必须挂 §2 指标,用数据验收
- **验收**:节点完成的唯一标准是过 ROADMAP 里的"验收"列,不是"代码能跑"

## 已安装

- superpowers 插件(project 作用域,`.claude/settings.json`);需**重启会话**后 SessionStart hook 生效
- 技术栈默认:优先 Claude Code + 轻量方案跑通,不预设重型平台(选型见 `docs/decisions`)

## 当前指针(随推进更新 — 上次更新 2026-09-08)

- 分支:`main`
- 进度:总纲 ✔ → L1 spec ✔ → 主执行图 ✔ → DEC-001~005 ✔ → G1 ✔(O1~O6 遗留待公司)→ G3 ✔ → G2 ✔ → 需求基线化 ✔(需求文档.md 事实源)→ **L1 M1.2 事实卡工具 ✔** → **L1 M2.1 选题卡+SEO ✔** → **DEMO 全链路演示 ✔**(虚构 MiraSol 跑通 M1.1/M1.3/M2.2/M2.3/L1-MVP/M3.1/M4.1,数据全标 demo,收口见 `docs/DEMO-交付说明.md`)
- **下一步**:为**生产**推进,待业务方供真实素材 G1-O3/O4(在售产品线+认证、素材样例)→ 以真实数据重跑 M1.1→M1.3→M2.2→L1-MVP;L2~L4 另待 O1/O2/O5/O6(见 DEMO 交付说明·生产就绪清单)
